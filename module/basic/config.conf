# 循环清理间隔（分钟）
clean_interval=1440

# 日志等级 0=DEBUG, 1=INFO, 2=WARN, 3=ERROR
log_level=1

# 日志轮转大小（MB）
log_max_size=10

# 日志轮转天数
log_max_age=7

# 循环清理开关
loop_clean_enable=true

# App触发清理开关
app_clean_enable=true

# App包名列表（从配置文件读取）
app_packages=bin.mt.plus.canary,bin.mt.plus